name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    # Hard ceiling so a stalled external call (e.g. astroquery/astropy reaching
    # a service) fails the job in minutes instead of hanging until GitHub's
    # 6-hour default and surfacing only as "The operation was canceled."
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip packages
        uses: actions/cache@v6
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Cache Lightkurve data
        uses: actions/cache@v6
        with:
          path: ~/.lightkurve/cache/mastDownload
          key: ${{ runner.os }}-lightkurve-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-lightkurve-

      - name: Remove corrupted FITS (if any)
        run: |
          find ~/.lightkurve/cache/mastDownload -type f -name "*.fits" -size -1M -delete || true

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,gui]"

      - name: Run tests
        # --timeout converts a hung test (e.g. an unmocked network call) into a
        # failure with a traceback pinpointing the stall, instead of wedging the
        # whole -n auto run. thread method is xdist-safe and cross-platform.
        run: pytest -v -m "not notebook and not network" -n auto --timeout=180 --timeout-method=thread

      - name: Verify package builds cleanly
        run: |
          pip install --upgrade build
          python -m build --sdist --wheel
